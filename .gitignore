# Secrets
/COMTRADE_API_KEY.json

# macOS
.DS_Store
**/.DS_Store

# Python cache
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Jupyter checkpoints
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Claude Code local config (personal settings, not project instructions)
.claude/
**/.claude/

# Data folder (all raw/external data — too large, not in repo)
data/

# Outdated legacy simulation (superseded by part_4_new_simulation)
part_4_simulation/

# Logs
pipeline_*.log
debug_*.log
pipeline_timing.log

# Zip archives
*.zip

# -------------------------------------------------------
# Large simulation outputs — too large for GitHub
# -------------------------------------------------------

# Ship location data (~1.2 GB per scenario, regenerable)
**/ship_locations.parquet

# Simulation checkpoints (~280-380 MB each, regenerable)
**/checkpoints/

# Pre-computed port-pair routes (111 MB each, regenerable via 00_precompute_routes.ipynb)
**/port_pair_routes.pkl

# Compat CSV exports (107-242 MB — same data is in the .parquet files)
**/compat/

# ships.parquet (46 MB each, regenerable from ship generation step)
part_4_new_simulation/simulation_output_data/*/ships.parquet

# Outdated archived results (large zips, superseded)
**/outdated_results/

# -------------------------------------------------------
# Large intermediate network-extraction checkpoints
# -------------------------------------------------------
part_3_network_extraction/network_outputs/checkpoints/G_*.gpickle
part_3_network_extraction/network_outputs/checkpoints/used_*.pkl

# -------------------------------------------------------
# Misc
# -------------------------------------------------------
Geospatial_testing.ipynb
